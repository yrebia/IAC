name: Deploy Dev Environment

on:
  push:
    branches:
      - dev
    tags:
      - 'v*.*.*'
  pull_request:
    branches:
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/GitHubActionsRole
          aws-region: eu-west-3

      # --- DEBUG IAM (Piste A) ---
      - name: Who am I? (STS)
        run: aws sts get-caller-identity

      - name: Show attached policies
        run: aws iam list-attached-role-policies --role-name GitHubActionsRole

      - name: Direct EKS call (debug)
        run: aws eks describe-cluster --name tmgr-eks --region eu-west-3 || true
      # ---------------------------

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.13.3

      - name: Terraform Format Check
        run: terraform fmt -check -recursive

      - name: Terraform Init
        run: terraform init -backend-config=backends/dev.config

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan -var-file=dev.tfvars

      - name: Terraform Apply
        run: terraform apply -var-file=dev.tfvars -auto-approve
