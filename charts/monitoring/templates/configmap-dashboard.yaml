apiVersion: v1
kind: ConfigMap
metadata:
  name: task-manager-dashboard
  labels:
    grafana_dashboard: "1"

data:
  task-manager-dashboard.json: |-
{{ .Files.Get "files/task-manager-dashboard.json" | indent 4 }}
